import random
from ui.text import set_footer, set_description, set_play_again, set_round_over

def start_round(phone, rng):
    # Reset UI elements using centralized helpers
    set_footer(phone, "")
    set_play_again(phone, False)
    set_round_over(phone, False)
    set_description(phone, "")

    # Additional logic for starting the round
    # ... rest of the function ...